'use_strict';
var 
    assert = require('assert'),
    testParent = require('../src/scripts/testParent.jsx');

describe('testParent', function() {
    it('should know that truth is undisputed', function() {
        assert.equal(true, true);
    });
});
